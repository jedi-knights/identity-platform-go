// Package ports defines the inbound and outbound port interfaces for the auth-server.
// Outbound port interfaces (repositories) live in the domain package to enforce the
// dependency rule: domain has no outward dependencies.
package ports
