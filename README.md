# identity-platform-go
A production-style OAuth2/OIDC reference in Go built with a pure microservices architecture. Each service is independently deployable with its own database, showcasing clean design, ports-and-adapters, and scalable authentication and authorization patterns.
